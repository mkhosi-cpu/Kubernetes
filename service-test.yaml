# k8s/service-test.yaml
apiVersion: v1
kind: Service
metadata:
  name: sample-app-service
  namespace: test
spec:
  type: LoadBalancer
  selector:
    app: sample-app
  ports:
  - port: 80
    targetPort: 80